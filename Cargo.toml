[package]
name = "finance-gui"
version = "0.1.0"
authors = ["sandiso"]
edition = "2021"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
slint = "1.4"
serde_json = "1.0"

[build-dependencies]
slint-build = "1.4"
